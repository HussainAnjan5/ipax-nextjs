'use client';
import React from 'react';

const AddtoCart = () => {
  return (
    <div>
      <button className='bg-black text-white rounded-lg p-3' onClick={() => console.log('Add to Cart')}>Add to Cart</button>
    </div>
  );
};

export default AddtoCart;
